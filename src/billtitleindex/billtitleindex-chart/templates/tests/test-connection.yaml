apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "billtitleindex-chart.fullname" . }}-test-connection"
  labels:
    {{- include "billtitleindex-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "billtitleindex-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
